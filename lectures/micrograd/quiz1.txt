1. **What is the purpose of the function `f(x)` defined at the beginning of the code?**

    a) It generates a random number for each x.
   
    b) It calculates a quadratic function for the given x value.
   
    c) It plots a graph of the function for a given range of x values.
   
    d) It computes the derivative of the function for the given x value.
    
   **Answer: b**

2. **What does the code `(f(x + h) - f(x))/h` calculate?**

    a) It computes the integral of the function `f` at the point `x`.
   
    b) It calculates the derivative of the function `f` at the point `x`.
   
    c) It calculates the area under the curve of the function `f`.
   
    d) It approximates the second derivative of the function `f` at the point `x`.
   
   **Answer: b**

3. **What is the `Value` class designed to represent in this code?**

    a) It represents the inputs and outputs of the neural network.
   
    b) It represents a node in a computational graph, storing data and gradient, and also defines how to compute gradient.
   
    c) It represents the weights and biases in a neural network.
   
    d) It represents the loss function to be minimized during training.
   
   **Answer: b**

4. **What does the `backward` method do in the `Value` class?**

    a) It performs the forward pass of the computational graph.
   
    b) It calculates the loss function for the given inputs.
   
    c) It performs the backward pass through the computational graph, calculating the gradients of all preceding nodes.
   
    d) It updates the weights and biases in the network based on the gradients.
   
   **Answer: c**

5. **What does the function `lol()` do in the code?**

    a) It calculates the gradient of a loss function with respect to a single parameter, `b`, using finite difference.
   
    b) It calculates the output of a multilayer perceptron for a given input.
   
    c) It trains a neural network for one epoch.
   
    d) It initializes the weights and biases of the network.
   
   **Answer: a**

6. **What does the `draw_dot` function do in the code?**

    a) It creates a visual representation of the computational graph using the `graphviz` library.
   
    b) It trains the model and updates the weights and biases.
   
    c) It performs a forward pass through the computational graph.
   
    d) It initializes the parameters of the computational graph.
   
   **Answer: a**

7. **What are `x1`, `x2`, `w1`, `w2`, and `b` in the latter part of the code?**

    a) They are the weights and biases of the neural network.
   
    b) `x1` and `x2` are inputs, `w1` and `w2` are weights, and `b` is a bias in a single neuron model.
   
    c) They represent the gradient of the loss function with respect to the inputs, weights, and biases.
   
    d) `x1` and `x2` are the outputs of the neural network, `w1` and `w2` are the inputs, and `b` is the learning rate.
   
   **Answer: b**
